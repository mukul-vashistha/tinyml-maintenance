# Task

Before running, predict which split gives logistic regression a higher average precision and write why.

After running, answer:

1. Did the result match your prediction?
2. Does the result make random rows a valid deployment test?
3. Which question does each split answer?
4. Why was random forest selected even though gradient boosting had a slightly higher test AP?
