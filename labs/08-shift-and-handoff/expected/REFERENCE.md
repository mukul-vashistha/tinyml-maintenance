# Reference explanation

On future timestamps, logistic regression reaches about 0.393 average precision and 88.2 percent recall at its validation-selected threshold.

On unseen machine type D, ROC-AUC is about 0.880 and average precision is about 0.410. At the transferred threshold, however, the model produces zero true positives and 40 false negatives. Recall is zero.

The ranking still contains information, but the existing action threshold is unusable for this group. The system should abstain or route the case to human review until a new policy is supported by separate validation data.
