# Task

An AI agent says:

~~~text
ROC-AUC remains high on machine type D, so the model is robust.
~~~

Review that claim using the threshold, true positives, false negatives, recall, and average precision. Decide whether the system should automate the action, change the policy, or abstain.

Finish with a handoff that names the remaining risk and one experiment that should run next.
