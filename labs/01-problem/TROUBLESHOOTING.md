# Troubleshooting

Do not confuse target_failure_now with target_failure_within_24h. The first describes the current simulated event. The second is the prediction target.

Do not list every dataframe column as evidence. Generator-only state is deliberately present so tests can prove that the model excludes it.

If the AI response starts proposing APIs, registries, or deployment services, ask which current acceptance check requires them.
