# Checkpoint

Hint: begin with what the operator knows at one timestamp, not with the model.

Answer these questions:

1. What exactly does a positive target mean?
2. Why is machine_id excluded even though it is known at prediction time?
3. Why are latent degradation and future temperature forbidden?
4. What is more expensive in this project: a false alarm or a missed failure?
5. Which decision still belongs to the engineer after AI writes the feature filter?
