# Troubleshooting

Do not apply the calibrated threshold to raw scores. The before-calibration and after-calibration scales are different.

The threshold is selected on one half of validation machines after calibration is fitted on the other half. The held-out test machines measure the final policy once.

Human review covers uncertain cases. It is not another model class.
