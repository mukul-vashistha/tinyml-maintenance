# Troubleshooting

Run the generate command if the CSV is missing. Run commands from the repository root.

Do not treat hidden degradation as a feature recommendation. It is useful for checking the simulator, but an operator would not observe it.

An EDA statement such as "temperature is higher for failures" is incomplete. Check how much the distributions overlap and state what you will test next.
